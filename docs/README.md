# Docs

Use this directory only for durable design notes that are too detailed for `AGENTS.md`.

The first implementation session should prefer `.task/MAIN.md` for active planning and keep docs focused on settled boundary decisions.
